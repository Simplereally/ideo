# Current-state parity fixes

## Issue 1: pending-video-jobs-strip.tsx - AUDIT RESULT
Looking at line 229-235, handleSelect already clears:
- selectImage(null) ✓
- selectJob(id) ✓

BUT it does NOT call selectImageJob(null) to clear the image job selection.
Compare to pending-image-jobs-strip.tsx lines 273-279 which clears:
- selectImage(null) ✓
- selectVideoJob(null) ✓  
- selectImageJob(id) ✓

**FIX NEEDED**: Add selectImageJob(null) to pending-video-jobs-strip.tsx

## Issue 2: history-panel.tsx - AUDIT RESULT
ImageJobItem (lines 351-478):
- Lines 380-394: Thumbnail retry affordance when canRetry ✓
- Lines 443-452: ALSO has hover retry action button when canRetry ← REDUNDANT

VideoJobItem (lines 224-349):
- Lines 253-267: Thumbnail retry affordance when canRetry ✓
- Lines 313-346: NO hover retry button ✓

**FIX NEEDED**: Remove the redundant hover retry button from ImageJobItem (lines 443-452)

## Issue 3: Accessibility/polish
- PendingCard in both strips has role="button" and tabIndex={0} ✓ 
- Video strip has aria-label="Cancel video generation" ✓
- Image strip has aria-label="Cancel generation" (less specific)

**FIX SUGGESTED**: Make aria-label consistent: "Cancel image generation"
